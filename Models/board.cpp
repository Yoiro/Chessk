#include "board.h"


Board::Board(QObject *parent): QObject(parent)
{
    for(int i = 0; i < cases.length(); i++){
        for(int j = 0; j < cases[i].length(); j++){
            cases[i][j]
        }
    }
}
